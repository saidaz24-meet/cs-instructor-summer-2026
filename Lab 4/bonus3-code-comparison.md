# Bonus 3 — Code Comparison: app.py vs Bolt-generated code

Fill this in after completing the lab. Find the API call in the code Bolt generated
and compare it to your `app.py` from Lab 2/3.

---

## Where to find the API call in Bolt

Look for a file called something like `server.js`, `api.js`, `route.ts`, or similar.
Search for the word `messages` or `anthropic` in the Bolt editor.

---

## 3 Differences

| # | app.py (Python) | Bolt-generated (JavaScript/TypeScript) |
|---|-----------------|----------------------------------------|
| 1 | | |
| 2 | | |
| 3 | | |

*Examples to look for: language used, how the API key is loaded, how history is stored,
what happens to the response before it reaches the user.*

---

## 3 Similarities

| # | What is the same |
|---|-----------------|
| 1 | |
| 2 | |
| 3 | |

*Examples to look for: the model name, the `messages` array structure,
`system` prompt as a separate field, `max_tokens`.*

---

## My deployed URL

```
https://
```

## What I noticed about AI-generated code

*(Write 2–3 sentences: Was it clean? Did you understand it? What would you change?)*
